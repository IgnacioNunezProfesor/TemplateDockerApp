CREATE SCHEMA IF NOT EXISTS `template_docker_app_dev` 
    DEFAULT CHARACTER SET utf8mb4 
    COLLATE utf8mb4_general_ci;
-- Crea el esquema si no existe, usando UTF-8 completo y una colación general compatible.

USE `template_docker_app_dev`; 
-- Selecciona el esquema para trabajar dentro de él.