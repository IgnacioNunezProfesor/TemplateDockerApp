# Mostrar información básica del sistema operativo en las cabeceras
ServerTokens OS

# Carpeta raíz del servidor Apache
ServerRoot /var/www

# Puerto donde Apache escuchará peticiones
Listen ${SERVER_PORT}

# Módulo MPM prefork (recomendado para PHP con mod_php)
LoadModule mpm_prefork_module modules/mod_mpm_prefork.so
# Módulos de autenticación y autorización
LoadModule authn_file_module modules/mod_authn_file.so
LoadModule authn_core_module modules/mod_authn_core.so
LoadModule authz_host_module modules/mod_authz_host.so
LoadModule authz_groupfile_module modules/mod_authz_groupfile.so
LoadModule authz_user_module modules/mod_authz_user.so
LoadModule authz_core_module modules/mod_authz_core.so
# Compatibilidad con reglas antiguas de acceso
LoadModule access_compat_module modules/mod_access_compat.so
# Autenticación básica HTTP
LoadModule auth_basic_module modules/mod_auth_basic.so
# Protección contra ataques de peticiones lentas
LoadModule reqtimeout_module modules/mod_reqtimeout.so
# Filtros de contenido
LoadModule filter_module modules/mod_filter.so
# Gestión de tipos MIME
LoadModule mime_module modules/mod_mime.so
# Configuración de logs
LoadModule log_config_module modules/mod_log_config.so
# Variables de entorno
LoadModule env_module modules/mod_env.so
# Manipulación de cabeceras HTTP
LoadModule headers_module modules/mod_headers.so
# Configuración condicional por entorno
LoadModule setenvif_module modules/mod_setenvif.so
# Permite condicionales según versión de Apache
LoadModule version_module modules/mod_version.so
# Gestión de permisos y usuario del proceso Apache
LoadModule unixd_module modules/mod_unixd.so
# Listado automático de directorios
LoadModule autoindex_module modules/mod_autoindex.so

# Carga condicional de módulos CGI según el MPM
<IfModule !mpm_prefork_module>
    #LoadModule cgid_module modules/mod_cgid.so
</IfModule>

<IfModule mpm_prefork_module>
    #LoadModule cgi_module modules/mod_cgi.so
</IfModule>

# Permite definir archivos por defecto (index.php, index.html)
LoadModule dir_module modules/mod_dir.so
# Permite crear alias de rutas
LoadModule alias_module modules/mod_alias.so
# Negociación de contenido (idiomas, formatos)
LoadModule negotiation_module modules/mod_negotiation.so

# Usuario y grupo bajo los que se ejecuta Apache
<IfModule unixd_module>
    User apache
    Group apache
</IfModule>

# Email del administrador del servidor
ServerAdmin you@${SERVER_NAME}

# Mostrar firma de Apache en páginas de error
ServerSignature On

# Nombre del servidor y puerto
ServerName ${SERVER_NAME}:${SERVER_PORT}

# Archivos por defecto al entrar en un directorio
<IfModule dir_module>
    DirectoryIndex index.html index.php info.php
</IfModule>

# Proteger archivos .htaccess y .htpasswd
<Files ".ht*">
    Require all denied
</Files>

# Archivo donde se guardan errores
ErrorLog logs/error.log

# Nivel de detalle de los logs
LogLevel warn

# Configuración de logs personalizados
<IfModule log_config_module>

    # Formato de log completo
    LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" combined

    # Formato de log básico
    LogFormat "%h %l %u %t \"%r\" %>s %b" common

    # Formato con bytes enviados/recibidos (si está activo mod_logio)
    <IfModule logio_module>
      # Necesita mod_logio para %I y %O
      LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\" %I %O" combinedio
    </IfModule>

    # Archivo donde se guardan accesos
    CustomLog logs/access.log combined
</IfModule>

# Alias para CGI
<IfModule alias_module>
    ScriptAlias /cgi-bin/ "/var/www/localhost/cgi-bin/"
</IfModule>

# Configuración del directorio CGI
<IfModule cgid_module>
    # Configuración del socket CGI (comentado)
    #Scriptsock cgisock
</IfModule>

<Directory "/var/www/localhost/cgi-bin">
    AllowOverride None
    Options None
    Require all granted
</Directory>

# Eliminar cabeceras Proxy para mayor seguridad
<IfModule headers_module>
    RequestHeader unset Proxy early
</IfModule>

# Configuración de tipos MIME
<IfModule mime_module>
    TypesConfig /etc/apache2/mime.types

# Tipos MIME adicionales
    AddType application/x-compress .Z
    AddType application/x-gzip .gz .tgz
</IfModule>

# Detección de tipos por contenido
<IfModule mime_magic_module>
    MIMEMagicFile /etc/apache2/magic
</IfModule>

# Incluir configuraciones adicionales
IncludeOptional /etc/apache2/conf.d/*.conf

