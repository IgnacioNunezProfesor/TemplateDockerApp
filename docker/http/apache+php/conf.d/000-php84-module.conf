LoadModule php_module modules/mod_php84.so # Carga el módulo de PHP 8.4 en Apache

DirectoryIndex index.php index.html info.php # Prioridad de archivos por defecto al entrar en un directorio

<FilesMatch \.php$> # Aplica esta regla a todos los archivos .php
    SetHandler application/x-httpd-php # Indica que los .php deben ser procesados por PHP
</FilesMatch>
